# Keep simulation mode while the backend endpoints are being implemented.
VITE_FRONTEND_MODE=simulation

# Spring Boot API origin used by the frontend during integration.
VITE_API_URL=http://localhost:8080/api
