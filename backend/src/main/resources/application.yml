spring:
  application:
    name: aguavigia-ctg
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/aguavigia}
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

server:
  port: ${SERVER_PORT:8080}
